import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingVertical: 8,
    borderRadius: 4,
  },
  labelText: {
    color: '#FFFFFF',
    fontSize: 16,
    lineHeight: 24,
  },
});
